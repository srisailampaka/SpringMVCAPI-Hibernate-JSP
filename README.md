# SpringMVCAPI-Hibernate-JSP
This is SpringMVC for Employee CRUD Operation using HIbernate, Mysql with JSP</br>
---> Import the Project into eclipse,then export to war file, place this war file in Tomcat webapp
